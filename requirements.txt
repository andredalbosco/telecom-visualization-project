# Mini-project Baby Names - Choropleth maps in Altair
# Python 3.12. Install with:  python -m pip install -r requirements.txt
#
# Versions are pinned with >= to the ones validated for this project so the
# notebook reproduces, while still allowing patch/minor upgrades.
# The executed notebooks keep Altair's rich text/html outputs so the sketches
# remain interactive; PNG export is still required for visual verification files.

altair>=5.5.0            # declarative charting (Vega-Lite)
pandas>=2.3.1            # dataframes / CSV loading
numpy>=2.2.6             # numerical transforms used by the 9 sketches
geopandas>=1.1.3         # geographic dataframes + GeoJSON reader
vl-convert-python>=1.9.0.post1 # PNG/SVG export for Altair verification artifacts

# Execution / debugging of the notebook from the command line
papermill>=2.6.0         # parametrised, headless notebook execution
ipykernel>=6.29.5        # "python3" kernel papermill runs against
nbconvert>=7.17.0        # used by the fallback path in run.bat
nbformat>=5.10.4         # notebook format support used by papermill/nbconvert
